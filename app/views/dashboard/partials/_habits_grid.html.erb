<!-- Habits Grid Partial -->
<div class="mb-8">
  <div class="flex items-center justify-between mb-6">
    <h2 class="text-2xl font-semibold text-text-primary">Today's Habits</h2>
  </div>
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
    <%= render partial: 'dashboard/partials/habit_card', collection: @habits, as: :habit %>
  </div>
</div>
