<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Habit Tracker" %></title>
    <meta name="description" content="Habit Tracker helps you create and track your daily habits to build consistency and reach your goals.">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="robots" content="index, follow">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <!-- Social preview -->
    <meta property="og:title" content="Habit Tracker">
    <meta property="og:description" content="Track your daily habits and stay consistent with Habit Tracker.">
    <meta property="og:image" content="https://habit-tracker-ohfv.onrender.com/icon.png">
    <meta property="og:url" content="https://habit-tracker-ohfv.onrender.com">
    <meta property="og:type" content="website">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet" />

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-gray-50 text-text-primary min-h-screen flex flex-col">
    <% unless turbo_native_app? %>
      <%= render "layouts/header" %>
    <% end %>
    <div class="grow flex flex-row-reverse overflow-hidden">
      <div class="grow overflow-y-auto scroller">
        <%= yield %>
      </div>
    </div>
    <%= render "shared/bottom_nav" %>
    <%= turbo_frame_tag 'flash_messages', class: 'hidden' do %>
      <div data-controller="notyf"></div>
      <% flash.each do |type, message| %>
        <% type = type.to_s %>
        <% next if flash[type].blank? %>
        <% next unless type.starts_with?('notyf_') %>
        <%= render 'shared/flash_message', type: %w[alert error].include?(type.gsub(/^notyf_/, '')) ? 'alert' : 'notice', message: message %>
      <% end %>
    <% end %>
    <%= render 'shared/slideover' %>
    <%= turbo_frame_tag 'slideover-content', class: 'hidden' %>
  </body>
</html>
