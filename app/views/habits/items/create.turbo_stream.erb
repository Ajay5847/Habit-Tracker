<% if @item.persisted? %>
  <%= turbo_stream.prepend "habits_list", partial: "habits/items/habit_list_card", locals: { item: @item } %>
  <%= turbo_stream.update "empty_state" do %>
    <%= render "habits/items/empty_state", empty_habits: false %>
  <% end %>
  <%= turbo_stream.append("flash_messages", partial: "shared/flash_message", locals: { type: "notice", message: "Habit item was successfully created." }) %>
  <% clode_modal %>
<% else %>
  <%= turbo_stream.replace 'habits_item_form', render('form', item: @item) %>
<% end %>