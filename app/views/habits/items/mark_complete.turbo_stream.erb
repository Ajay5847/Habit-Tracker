<%= turbo_stream.remove dom_id(@item) %>
<% if @in_progress_habits_count.zero? %>
  <%= turbo_stream.update "habit_items_inprogress" do %>
    <%= render "habits/items/empty_inprogress", completed_habits_count: @completed_habits_count %>
  <% end %>
<% end %>

<%= turbo_stream.append("flash_messages", partial: "shared/flash_message", locals: { type: "notice", message: "Great job on completing your habit...🎉" }) %>

<%= turbo_stream.remove("empty_completed") %>
<%= turbo_stream.append "habit_items_completed", partial: "habits/items/habit_card", locals: { item: @item } %>